'use client';

export default function RecommendedBadge() {
  return (
    <span className="inline-flex items-center rounded-full bg-emerald-50 px-3 py-0.5 text-xs font-semibold uppercase tracking-wide text-emerald-700">
      Recommended
    </span>
  );
}
